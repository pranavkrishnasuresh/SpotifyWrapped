<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotifyTutorialTrialRun"
        tools:targetApi="31">

        <!-- Declare WelcomeScreenActivity with the MAIN action and LAUNCHER category -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare LoginActivity without intent filters, as it's not the first screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".HomeActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".WrappedActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".FriendsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".InviteFriendsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".FriendRequestsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".HistoryActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".PastWrappedActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ChangePasswordActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>